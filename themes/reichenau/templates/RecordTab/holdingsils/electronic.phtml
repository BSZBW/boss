<h2><?=$this->transEsc('Electronic')?></h2>
<?php foreach ($this->holdings as $holding): ?>
    <a class="external" href="<?=$holding['url']?>" target="_blank"><?=$holding['desc'] ?? $holding['url']?></a>
    <br>
<?php endforeach; ?>